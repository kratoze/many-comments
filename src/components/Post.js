import React from 'react';
import { Flex, Box } from '@chakra-ui/core';

import InteractionBar from './interactionbar';

const Post = (props) => {
  return <Flex>Helsdfsdflo</Flex>;
};

export default Post;
